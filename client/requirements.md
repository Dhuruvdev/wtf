## Packages
framer-motion | Essential for smooth animations in the game interface
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
The application is a Discord Activity.
Real-time state is handled via WebSocket at /ws.
Uses 'wouter' for routing.
Default to Dark Mode.
